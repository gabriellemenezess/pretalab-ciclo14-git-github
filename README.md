# pretalab-ciclo14-git-github
1° projeto - PretaLab
# Comandos para Git e GitHub: do básico ao avançado
  
  git status

  git add nome-do-arquivo

  git commit -m "mensagem"

  git log

  git diff

  git push  -u origin Main

  git push origin main ou git push

  git restore nome-do-arquivo

  git restore --staged nome-do-arquivo

  git  reset --soft HEAD~1

  git branch
  
  git checkout -b nome

  git checkout nome

  git clone <url>
  
  git fetch
 	
  git pull

  git push

  git merge nome
  
  git rebase main


  
